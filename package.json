{
  "name": "client-server-template-with-vite",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && node init.js && lerna clean && yarn && lerna bootstrap",
    "build": "lerna run build",
    "check:all": "./scripts/check-build-all.sh",
    "check:types": "./scripts/check-types.sh",
    "check:production": "./scripts/check-production.sh",
    "dev:client": "lerna run dev  --scope=client",
    "dev:server": "lerna run dev --scope=server",
    "dev": "lerna run dev",
    "preview:client": "lerna run preview --scope=client",
    "preview:server": "lerna run preview --scope=server",
    "preview": "./scripts/preview-all.sh",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "format": "lerna run format"
  },
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=15"
  },
  "devDependencies": {
    "@evilmartians/lefthook": "^1.3.9",
    "lerna": "^5.4.3"
  }
}
